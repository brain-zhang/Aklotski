<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.happy123.aklotski"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="me.happy123.aklotski.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.happy123.aklotski.ChessBoardActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="me.happy123.aklotski.LevelsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="me.happy123.aklotski.WarsActivity"
            android:label="@string/app_name" >
        </activity> 
        <activity
            android:name="me.happy123.aklotski.HelpActivity"
            android:label="@string/app_name" >
        </activity>         
		<service 
            android:name=".BMusicService" />        
    </application>

</manifest>
